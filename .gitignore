# Rust build artefacts
/target
debug
# rustfmt backup files
**/*.rs.bk
# MSVC debug info
*.pdb
# cargo-mutants output
**/mutants.out*/

# reference papers
*.pdf

# Benchmark output — regenerate via scripts/bench.sh; not tracked.
# `scripts/bench.sh` writes to results/<crate>/; a bare `cargo bench` falls
# back to a crate-local results/ (ignored too, as a safety net).
/results/
/segmented-cuckoo/results/
/ikpir-server/results/
/ikpir-client/results/

# Editors / OS
.DS_Store
*.swp
.idea/
.vscode/

# Local cargo config
.cargo/config.toml
